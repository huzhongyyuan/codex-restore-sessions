interface:
  display_name: "Restore Codex Sessions"
  short_description: "Diagnose and safely restore Codex sessions in one command"
  default_prompt: "Use $codex-restore-sessions to run a read-only session health check and recommend or perform a guarded restore."
