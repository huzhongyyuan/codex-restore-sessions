interface:
  display_name: "Restore Codex Sessions"
  short_description: "Diagnose and safely restore Codex sessions in one command"
  default_prompt: "Use $codex-restore-sessions to quickly check or restore my local Codex sessions with the guarded zero-configuration workflow."
